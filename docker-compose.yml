services:
  backend:
    build: ./backend
    container_name: hackathon-backend
    restart: unless-stopped
    ports:
      - "5001:5001"
    env_file:
      - ./backend/.env
    networks:
      - shared-network

networks:
  shared-network:
    external: true